# CodeRabbit 설정
# 기본값은 default 브랜치(main)로 향하는 PR만 자동 리뷰한다.
# 이 프로젝트는 모든 기능 브랜치를 dev로 먼저 PR 보내고, 배포 시점에만 dev -> main PR을 내므로
# 두 브랜치 모두 자동 리뷰 대상으로 등록한다.
reviews:
  base_branches:
    - "main"
    - "dev"
